// Barrel re-export preserved for backward compatibility. All entity types
// now live under src/types/{store,wire}/. New code should import directly
// from ./types/store/*.ts or ./types/wire/*.ts.
export * from "./types/index.js";
