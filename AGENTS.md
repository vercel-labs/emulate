# Agents

## Rules

- Check for and use latest dependency versions
- When changing user-facing behavior (routes, seed config, CLI options, SDK integration), update the corresponding docs: skills (`skills/*/SKILL.md`), README, and web app docs (`apps/web/`)
